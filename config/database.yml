default: &default
  adapter: postgresql
  encoding: utf8
  pool: 5

development:
  <<: *default
  host: localhost
  database: tinylog
  username: tinyusr
  password: password

test:
  url: <%= ENV["DATABASE_URL"] %>

production:
  url: <%= ENV["DATABASE_URL"] %>
